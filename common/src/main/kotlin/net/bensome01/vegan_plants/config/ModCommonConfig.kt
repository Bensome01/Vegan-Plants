package net.bensome01.vegan_plants.config

import net.neoforged.neoforge.common.ModConfigSpec

class ModCommonConfig (builder: ModConfigSpec.Builder) {
    init {
        builder.push("There are only Server configs")
    }
}
